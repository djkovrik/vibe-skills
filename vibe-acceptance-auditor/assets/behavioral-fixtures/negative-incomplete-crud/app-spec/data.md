# Data

Every state-changing operation must persist and survive reload.

## Localized text storage

Persist stable entity IDs and resource keys, never resolved EN/RU translations. Native resources provide the EN fallback.
