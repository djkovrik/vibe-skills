# Data

The fixture keeps the preference in memory and exposes all CRUD operations.

## Localized text storage

Persist only stable preference IDs. Bundled labels use shared Compose resource keys with native EN fallback.
