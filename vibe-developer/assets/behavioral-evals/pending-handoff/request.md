# Pending hand-off after compaction

Conversation is unavailable. Resume finds an immutable specialist hand-off whose result fingerprint is current and whose changed files remain inside the active AC boundaries, but its SHA-256 is not in the ledger. State the verdict and actions.
